# Android target placeholder

Do not copy or alter a QIDK sample application in place. Create the Android project here only after the host replay, feature order, safe report contract, and V75 Genie smoke test are proven.

Use `../../qidk/GenAI-Solutions/AI-Assistant` as a read-only reference for Genie C++/JNI lifecycle and runtime libraries. The app must replay the same compact JSONL fixtures and write the same trace schema as the host pipeline.

